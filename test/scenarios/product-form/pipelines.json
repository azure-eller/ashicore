{
  "pipelines": [
    {"field": "name", "input": "User types a string", "formState": "string (default: '' for create, initialData.name for edit)", "zodTransform": "NONE", "zodValidation": "z.string().min(1, 'Name is required')", "apiHandler": "Passed directly to DAL", "dalFunction": "inserted/updated in items table", "dbColumn": "name — varchar(255), NOT NULL"},
    {"field": "description", "input": "User types string or leaves empty. Renders field.value ?? ''", "formState": "string | null | undefined", "zodTransform": "nullableString: trims, empty becomes null", "zodValidation": "NONE beyond transform", "apiHandler": "Passed directly to DAL", "dalFunction": "inserted/updated in items table", "dbColumn": "description — text, nullable"},
    {"field": "sku", "input": "User types string or leaves empty. Renders field.value ?? ''", "formState": "string | null | undefined", "zodTransform": "nullableString: trims, empty becomes null", "zodValidation": "NONE beyond transform", "apiHandler": "Passed directly to DAL", "dalFunction": "inserted/updated in items table", "dbColumn": "sku — varchar(50), nullable. Partial unique index"},
    {"field": "category", "input": "User selects or types. onValueChange passes (v || null)", "formState": "string | null | undefined", "zodTransform": "nullableString: trims, empty becomes null", "zodValidation": "NONE beyond transform", "apiHandler": "Passed directly to DAL", "dalFunction": "inserted/updated in items table", "dbColumn": "category — varchar(100), nullable"},
    {"field": "itemType", "input": "NOT user-editable — set from itemType prop", "formState": "string", "zodTransform": "NONE", "zodValidation": "z.enum(['product', 'material'])", "apiHandler": "CREATE only", "dalFunction": "inserted in items table", "dbColumn": "item_type — varchar(20), NOT NULL, default 'material'"},
    {"field": "unitDefinitionId", "input": "User selects from dropdown or creates via dialog. Create only", "formState": "string (default: '')", "zodTransform": "NONE", "zodValidation": "z.string().min(1, 'Unit is required')", "apiHandler": "CREATE only", "dalFunction": "inserted in items table, FK to unit_definitions", "dbColumn": "unit_definition_id — uuid, NOT NULL, FK"},
    {"field": "defaultPurchasePrice", "input": "User types decimal or leaves empty", "formState": "string | null | undefined", "zodTransform": "nullableString: trims, empty becomes null. NO conversion to number", "zodValidation": "NONE — no numeric validation", "apiHandler": "Passed as string | null", "dalFunction": "inserted/updated + used as lot costPerUnit on create", "dbColumn": "default_purchase_price — numeric(10,4), nullable"},
    {"field": "defaultSellingPrice", "input": "User types decimal or leaves empty", "formState": "string | null | undefined", "zodTransform": "nullableString: trims, empty becomes null. NO conversion to number", "zodValidation": "NONE — no numeric validation", "apiHandler": "Passed as string | null", "dalFunction": "inserted/updated in items table", "dbColumn": "default_selling_price — numeric(10,2), nullable"},
    {"field": "stock", "input": "User types number", "formState": "string (default: '0')", "zodTransform": "z.string().default('0')", "zodValidation": "refine: !isNaN(Number(v)) && Number(v) >= 0", "apiHandler": "CREATE: passed to createItemWithLot. UPDATE: parseFloat, compute delta", "dalFunction": "Creates lots + stock movements. On update: adjustStockInTx with delta", "dbColumn": "Derived from SUM(lots.quantity). lots.quantity — numeric(12,4)"},
    {"field": "safetyStock", "input": "User types number", "formState": "string (default: '0')", "zodTransform": "z.string().transform(v => v.trim() === '' ? '0' : v) — NO numeric validation", "zodValidation": "NONE", "apiHandler": "Passed as string", "dalFunction": "inserted/updated in items table", "dbColumn": "safety_stock — numeric(12,4), NOT NULL, default '0'"},
    {"field": "bomMode", "input": "User selects 'quantity' or 'percentage'. Products only", "formState": "'quantity' | 'percentage' (default: 'quantity')", "zodTransform": "NONE", "zodValidation": "z.enum(['quantity', 'percentage']).nullable().optional()", "apiHandler": "Passed directly. Used in bomRefine to determine which BOM field is required", "dalFunction": "inserted/updated in items table", "dbColumn": "bom_mode — varchar(20), nullable"},
    {"field": "bom[n].componentId", "input": "User selects from combobox", "formState": "string (default: '')", "zodTransform": "NONE", "zodValidation": "z.string().min(1, 'Component is required') + bomRefine duplicate check", "apiHandler": "Self-reference check on update. Passed to DAL", "dalFunction": "CREATE: insert bomComponents. UPDATE: delete all + re-insert", "dbColumn": "component_id — uuid, NOT NULL, FK to items(id), unique with item_id"},
    {"field": "bom[n].quantity", "input": "User types decimal when bomMode='quantity'", "formState": "string | null (default: null)", "zodTransform": "nullableString: trims, empty becomes null", "zodValidation": "bomRefine: required when bomMode='quantity'. No numeric validation", "apiHandler": "Passed as string | null", "dalFunction": "inserted in bomComponents", "dbColumn": "quantity — numeric(12,4), nullable"},
    {"field": "bom[n].percentage", "input": "User types decimal when bomMode='percentage'", "formState": "string | null (default: null)", "zodTransform": "nullableString: trims, empty becomes null", "zodValidation": "bomRefine: required when bomMode='percentage'. No numeric or range validation. UI warns if total != 100% but does NOT block", "apiHandler": "Passed as string | null", "dalFunction": "inserted in bomComponents", "dbColumn": "percentage — numeric(5,2), nullable"}
  ]
}
