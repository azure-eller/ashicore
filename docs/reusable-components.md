---
read_when:
  - Before creating a React component
status: archived-pointer
---

# Reusable Components

This catalog moved to `docs/design/components.md`.

Keep component selection guidance in `docs/design/components.md`; keep props and
implementation details in TypeScript source files. Do not add a second component
catalog here.
