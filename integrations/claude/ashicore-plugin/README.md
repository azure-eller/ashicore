# Ashicore Claude Plugin

Adds one Ashicore production schedule skill and references the Ashicore MCP server.

MCP server:

```txt
https://ashicore.app/api/agent/mcp
```

Use the skill when asking Claude what to make each day or whether shipments will be missed.
